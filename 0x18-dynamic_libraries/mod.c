#include <stdio.h>

/**
 * mod - This function return the modulus of two elements
 * @a: The first element
 * @b: The second element
 * Return: Modulus
 */

int mod(int a, int b)
{
	return (a % b);
}
