#include "main.h"

/**
 * more_numbers - print more numbers
 * Return: void
 */

void more_numbers(void)
{
	int a, b;

	for (a = 1; a < =10; i++)
	{
		for (a = 0; b <= 14; j++)
		{
			if (b >= 10)
				_putchar('1');
			_putchar(j % 10 + '0');
		}
		_putchar('\n');
	}
}

